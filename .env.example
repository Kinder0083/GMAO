# ==================================
# GMAO Atlas - Configuration
# ==================================

# Database Configuration
MONGO_USER=admin
MONGO_PASSWORD=change_this_password
DB_NAME=gmao_atlas

# JWT Configuration (générez une clé sécurisée avec: openssl rand -hex 32)
JWT_SECRET_KEY=your_jwt_secret_key_change_in_production

# Application URLs
PUBLIC_API_URL=http://localhost:8001
PUBLIC_FRONT_URL=http://localhost:3000

# Email Configuration (optionnel)
ENABLE_EMAIL_NOTIFICATIONS=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PWD=

# ==================================
# Instructions:
# 1. Copiez ce fichier vers .env
# 2. Modifiez les valeurs selon votre configuration
# 3. Ne commitez jamais le fichier .env !
# ==================================
